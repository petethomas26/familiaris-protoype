{% if flash.getMessage('info') %}

	<div class="alert alert-success" role="alert">
		{{ flash.message('info') | first }}
	</div>

{% endif %}

{% if flash.getMessage('error') %}

	<div class="alert alert-warning" role="alert">
		{{ flash.message('error') | first }}
	</div>

{% endif %}
